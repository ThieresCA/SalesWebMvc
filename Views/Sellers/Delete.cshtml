@model SalesWebMvc.Models.Seller
@{
    ViewData["Title"] = "Delete";
}

<h2>@ViewData["Title"]</h2>

<h3>Tem certeza que quer deletar esse registro?</h3>

<div>
    <h4>Seller</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @*Esse mostra o nome do atributo ou do display dado dentro do model utilizado*@
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @*Esse mostra o valor de dentro do atributo*@
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @*Esse mostra o nome do atributo ou do display dado dentro do model utilizado*@
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd>
            @*Esse mostra o valor de dentro do atributo*@
            @Html.DisplayFor(model => model.Email)
        </dd>
        <dt>
            @*Esse mostra o nome do atributo ou do display dado dentro do model utilizado*@
            @Html.DisplayNameFor(model => model.BirthDate)
        </dt>
        <dd>
            @*Esse mostra o valor de dentro do atributo*@
            @Html.DisplayFor(model => model.BirthDate)
        </dd>
        <dt>
            @*Esse mostra o nome do atributo ou do display dado dentro do model utilizado*@
            @Html.DisplayNameFor(model => model.DepartmentId)
        </dt>
        <dd>
            @*Esse mostra o valor de dentro do atributo*@
            @Html.DisplayFor(model => model.Department.Name)
        </dd>
        <dt>
            @*Esse mostra o nome do atributo ou do display dado dentro do model utilizado*@
            @Html.DisplayNameFor(model => model.BaseSalary)
        </dt>
        <dd>
            @*Esse mostra o valor de dentro do atributo*@
            @Html.DisplayFor(model => model.BaseSalary)
        </dd>
    </dl>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        @if (!Model.Active)
        {
            <input type="submit" asp-action="Delete" class="btn btn-danger" value="Ativar" />
        }
        else
        {
            <input type="submit" asp-action="Delete" class="btn btn-danger" value="Inativar" />
        }
    </form>
</div>
<div style="margin-top: 10px;">
    <a asp-action="Index" style="margin-right: 5px">Voltar para o Menu</a>
</div>

